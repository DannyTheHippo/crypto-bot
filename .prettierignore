dist
coverage
.pnpm-store
.pnpm-cache
pnpm-lock.yaml
drizzle/meta
# Harness agent worktrees — full repo copies under .claude/worktrees.
.claude

# Markdown is owned by markdownlint (.markdownlint-cli2.jsonc), not prettier — prettier's reflow
# mangles long-form technical prose. See `pnpm lint:md`.
*.md

# Generated by SwaggerModule.createDocument via test/unit/config/openapi-snapshot.spec.ts
# (OPENAPI_WRITE=1) — JSON.stringify(doc, null, 2) output, not prettier's json formatting.
openapi.json

# Fetched market-data caches (gitignored; raw fetch-data.mjs output, not hand-formatted).
test/backtest/data/ohlcv-*.json
test/backtest/data/funding-*.json

# Candidate-playbook exports (byte-exact agent_playbook_versions copies — the experiments
# registry's params_hash is the sha256 of those bytes) and their eval-written scorecards
# (AGENTIC_EVAL_SCORECARD_FILE output). Artifacts, not authored code; also excluded from
# markdownlint (.markdownlint-cli2.jsonc).
research/candidates
research/scorecards

# Y3 collector / Y2 sweep runtime output (single-line JSONL, timestamped sweep JSONs, rendered
# md digests) — machine-written by scripts/loop-collect.mjs & loop-sweep.mjs, rewritten at
# runtime; formatting them churns the collector's own files. Gitignored; also excluded from
# markdownlint (.markdownlint-cli2.jsonc).
research/loop/digests
