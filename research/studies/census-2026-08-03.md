# Population census — 2026-08-03T18:25Z

_The shared denominator ledger for the distrust-everything pass. Every later step in this pass cites
this file instead of re-counting. Read-only probe against the live `cryptobot` database via
`docker exec crypto-bot-postgres-1 psql`._

**As-of:** 2026-08-03T18:25:30Z (fills/decisions), 18:27:06Z (positions). HEAD `8c753d2`.
**Epoch used:** `PROMOTION_EVIDENCE_EPOCH` = 1784632860000 = 2026-07-21T11:21:00Z.

> **Timestamp trap, recorded because it cost a round trip here:** `fills.venue_timestamp` and
> `agent_decisions.event_time` are `BIGINT` epoch-milliseconds, not `timestamptz`. A
> `where venue_timestamp >= timestamptz '...'` comparison does not silently coerce — it errors. Any
> later probe must pass epoch-ms integers. Two constants were mis-transcribed on the first attempt
> here and produced a window four days short; both were caught by round-tripping the constant back
> through `to_timestamp()` before using it, which is now the standing check.

## 1. Fee schedule, measured from fills since epoch (feeds R5 and the break-even bar)

| venue | liquidity | fills | notional (USDT) | bps/leg |
| --- | --- | --- | --- | --- |
| `binance` (spot) | maker | 3 | 160.4085 | 10.000000 |
| `binance` (spot) | taker | 13 | 747.0353 | 10.000000 |
| `binanceusdm` (perp) | maker | 194 | 2779.3401 | 1.999998 |
| `binanceusdm` (perp) | taker | 85 | 4586.9732 | 4.528056 |

| scope | fills | notional | bps/leg |
| --- | --- | --- | --- |
| ALL | 295 | 8273.7572 | 4.278973 |
| `binance` | 16 | 907.4438 | 10.000000 |
| `binanceusdm` | 279 | 7366.3134 | 3.574209 |

**The load-bearing number of this pass.** The perp venue's realised blended cost is **3.574 bps/leg
⇒ ~7.15 bps/round trip**. The strategy lane charges every symbol a hardcoded 10/10 spot schedule
⇒ a **20 bps** take-profit floor. The floor is therefore **2.8×** the true perp round trip, and the
book is **89.0% perp by notional** (7366.31 of 8273.76) — higher than the 85% previously recorded.
Every perp take-profit between ~7 bps and 20 bps has been vetoed on a false cost, and the system
prompt told the model the false number.

Fee currencies: USDT 287, AAVE 3, BTC 2, ZEC 2, SOL 1 (295 fills all-time). `fills.fee_resolved` is
**false on all 295** — vestigial, as previously recorded. `intent_id` null on **0** fills;
`fee_amount` null on **0**.

## 2. Decision journal

| quantity | n |
| --- | --- |
| `agent_decisions` rows, all | 43,312 |
| non-replay | 43,312 (no replay rows have ever been journalled here) |
| `trigger_kind='candle'` | 43,312 |
| rows carrying `input_payload` | **1,731** |
| model-answered (`prompt_hash<>''` and `latency_ms` not null) | 1,412 |
| rows with `consult_id` | 1,412 |
| rows with token columns | 659 |
| first → last `event_time` | 1784632500000 → 1785780000000 |

By action (all `trigger_kind='candle'`): `hold` 42,498 (1,543 with payload) · `error` 626 (0 with
payload) · `open_long` 56 · `close` 51 · `adjust` 45 · `open_short` 36. Every entry row carries a
payload (92 of 92).

Token totals by model: `claude-sonnet-5` 2,085 rows — in 4,261,952 · out 399,403 · cache-read
4,902,271 · cache-create 508,748. `prescreen` 39,472 rows and `plan-executor` 1,755 rows carry zero
tokens by construction.

Degrade-tag census over `hold`/`error` rows: `client_latched` **441** · `schema_rejected` **176** ·
`truncated_max_tokens` **35** · `plan_authoritative_close` 6 · other 42,466. The first three are
exactly the rows the noop-breaker currently miscounts as model-authored idling.

## 3. Entries and the entry-rate denominator (feeds R6)

| venue | action | n |
| --- | --- | --- |
| `binance` | `open_long` | 14 |
| `binanceusdm` | `open_long` | 42 |
| `binanceusdm` | `open_short` | 36 |

**92 lifetime entries**, not the **n=64** the record carries — the record is stale, not wrong-at-
authorship. Denominator candidates: 43,312 all candle rows; **981** rows that are both
payload-bearing and show `"position":{"side":"FLAT"`; 1,731 payload-bearing rows.

Under the definition this pass registers — entries ÷ FLAT-position consult rows —
**entry rate = 92 / 981 = 9.38%**. That is materially below the **13.29%** figure circulating in the
record, which an earlier search already established is absent from the repo. R6 owns the formal
record; this is the raw count it starts from.

**Spot is not entry-dead.** The record's "191 consults → 0 entries" reads as though the spot half
never trades. Lifetime, spot has **14 `open_long` entries**. Any expectancy argument for retiring
the spot menu must be rebuilt on this number, and must state its own window explicitly — the
zero-entry claim was scoped to a post-v10 window, and this census is lifetime.

## 4. Payload block presence (feeds R3 feasibility)

Text-scan counts (`LIKE '%"block"%'` — approximate by construction, but it cannot throw on the
`TEXT` column the way a `::jsonb` cast can).

| block | all payload rows (n=1,731) | entry rows only (n=92) |
| --- | --- | --- |
| `orderBook` | 1,731 | — |
| `bookStructure` | 1,412 | **92 (100%)** |
| `liquidation` | 1,730 | **92 (100%)** |
| `positioning` | 1,121 | **78 (84.8%)** |
| `derivatives` | 1,121 | **78 (84.8%)** |
| `fundingHistory` | 1,121 | — |
| `tradeFlow` | 610 | **14 (15.2%)** |
| `fearGreed` | 1,731 | — |
| `crossSymbol` | 1,407 | — |
| `trackRecord` | 169 | — |
| `similarSetups` | **0** | **0** |

Consequences R3's pre-registration must carry:

- **`tradeFlow` fails the 80% presence bar on entry rows (15.2%) and drops from the family**, with
  the exclusion disclosed rather than silently averaged.
- `bookStructure` and `liquidation` are present on every entry row; `positioning` and `derivatives`
  on ~85%. Four families survive, not five.
- **`similarSetups` has never rendered in a single payload, ever (0 of 1,731).** Independent
  confirmation of the episodic-memory finding from the live journal rather than from code reading.

## 5. Family B corpus trigger

**584 qualifying rows** (non-replay, `trigger_kind='candle'`, payload present, FLAT marker,
`event_time >= 2026-07-30T09:01:00Z`) against a required **≥354**. **The trigger is met.** A
planning estimate in this pass put the count near 255 and predicted a wait until ~2026-08-05; that
estimate is refuted — the run is unblocked on corpus grounds and gated only on its dated re-budget
record and interpretation contract.

## 6. Table inventory

`orders` 483 · `order_intents` 483 · `order_events` 1,586 · `audit_log` 32,823 ·
`reconciliations` 39,195 · **`config_snapshots` 1** · `experiments` 15 · **`funding_events` 0** ·
`funding_payments` 54 · `llm_usage` 69 · **`fee_ledger` 0**.

- **Task #104 is RESOLVED, not open.** `config_snapshots` holds one row (hash
  `3a1221892e2bda0c…`, mode `testnet`, activated 2026-08-03T17:00:04Z) — the writer shipped in
  `118132c`. The recorded "zero rows and zero writers" is stale.
- `funding_events` 0 confirms the append-only trigger guards a table with no writer.
- `fee_ledger` 0 and `fee_resolved` false-on-all confirm both surfaces remain vestigial.

## 7. Reconciliation axes — the dead perp axis, worse than recorded

| venue | result | passes | open-orders checked | **trades checked** | latest |
| --- | --- | --- | --- | --- | --- |
| `binance` | CLEAN | 16,928 | 2,616 | 61,755 | 2026-08-03T18:25:27Z |
| `binance` | MISMATCH | 2,673 | 2 | 14 | 2026-07-30T19:00:30Z |
| `binance` | HALT | 8 | 0 | 0 | 2026-07-27T16:45:23Z |
| `binanceusdm` | CLEAN | 16,641 | 12,242 | **0** | 2026-08-03T18:25:44Z |
| `binanceusdm` | HALT | 1,720 | 2 | **0** | 2026-07-31T17:30:59Z |
| `binanceusdm` | MISMATCH | 1,226 | 72 | **0** | 2026-08-03T17:46:00Z |

Last 24 h: `binance` 1,434 passes / 11,403 trades checked; `binanceusdm` 1,433 passes / **0**.

**This is not a 13-hour outage. `binanceusdm` has never checked a single trade in any of its 19,587
lifetime passes**, while its open-orders axis runs normally (12,242 checked). The fill-backfill and
the fill-for-unknown-order corruption detector have therefore been inert since inception on the
venue that holds 89% of the book's notional and both current real positions. The zero is structural,
not a window artifact.

## 8. Positions and equity

Six open positions:

| strategy | venue | symbol | signed qty | avg entry | notional |
| --- | --- | --- | --- | --- | --- |
| `agentic-26` | `binanceusdm` | ETH/USDT:USDT | −0.043 | 1859.20 | ~$79.9 |
| `agentic-30` | `binanceusdm` | NEAR/USDT:USDT | −46.0 | 1.733 | ~$79.7 |
| `agentic-7` | `binance` | AAVE/USDT | 0.000649 | 96.34 | **~$0.06** |
| `agentic-1` | `binance` | BTC/USDT | 0.00000755 | 64627.97 | **~$0.49** |
| `agentic-3` | `binance` | SOL/USDT | 0.000476 | 76.21 | **~$0.04** |
| `agentic-6` | `binance` | ZEC/USDT | 0.000736 | 476.67 | **~$0.35** |

**The four spot positions are dust** — every one is far below `PROMOTION_DUST_NOTIONAL=5`, together
worth about **$0.94**. They are the four permanently-pinned menu slots the recorded defect describes,
confirmed live rather than inferred. The two real positions are both perp shorts, both on the venue
whose trades axis is dead (§7).

Latest equity sample (2026-08-03T18:26:06Z): equity 4966.175080, cash 5126.069094, unrealized
−1.167984, peak 5000.695479, mode `testnet`, `gap_annotation` empty.

15 distinct symbols / 12 distinct base assets traded since epoch — the cluster-bootstrap floor of
`MIN_CLUSTERS=5` is comfortably satisfiable on bases, and 12 is the true ceiling for any
cluster-count claim in this window.

## What this census cannot answer

- **It does not close a single round trip.** §11 of the probe counted 164 dust-crossings under a
  signed-qty running sum, which is _not_ the production cycle rule — `walkRoundTrips` carries a
  stateful `peakNotional` guard that this SQL has no equivalent for, and reproducing it here would
  create a second source of truth for the number the promotion gate returns. The 48/46/38
  reconciliation is R9's job, using the imported production function.
- Block-presence counts are text scans, so a block name appearing inside a string value would
  inflate them. They are census-grade, not analysis-grade; R3 must re-extract with a guarded cast.
- Fee bps convert non-quote fees at that fill's own price, matching the production rule, but 8 of
  295 fills are non-USDT and the conversion inherits their price stamp.
- Nothing here measures forward returns, edge, or the passive benchmark.
